<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.my.mytask">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Home">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".gallery" />
        <activity android:name=".ferrari" />
        <activity android:name=".lamborghini" />
        <activity android:name=".BMW" />
        <activity android:name=".maserati" />
        <activity android:name=".audi" />
        <activity android:name=".buggati"></activity>
    </application>

</manifest>